version: '3.8'

services:
  bot:
    build: .
    volumes:
      - ./logs:/app/logs
      - ./database.db:/app/database.db
      - ./assets:/app/assets
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
    command: python main.py
    restart: unless-stopped
