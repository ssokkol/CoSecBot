version: '3.8'

services:
  bot:
    build: .
    volumes:
      - ./logs:/app/logs
      - ./club.db:/app/club.db
      - ./assets:/app/assets
    env_file:
      - .env
    command: python main.py
    restart: unless-stopped
