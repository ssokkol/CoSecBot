version: '3.8'

services:
  bot:
    build: .
    volumes:
      - ./database.db:/app/database.db
      - ./assets:/app/assets
      - ./bot.log:/app/bot.log
    env_file:
      - .env
    restart: unless-stopped
